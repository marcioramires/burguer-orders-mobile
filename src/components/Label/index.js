import React from "react";

import { Label as PageLabel } from "./styles";

// eslint-disable-next-line react/prop-types
function Label({ children }) {

    return <PageLabel>{children}</PageLabel>
}

export default Label